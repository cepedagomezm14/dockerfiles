services:
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n_app
    restart: always
    ports:
      - '5678:5678' # acceso web a n8n
    networks:
      - n8n_net

networks:
  n8n_net:
    driver: bridge
